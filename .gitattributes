Content/Art/** filter=lfs diff=lfs merge=lfs -text

# Unreal Engine – LFS only for large binary assets

# Core Unreal binary assets (always heavy)
*.uasset    filter=lfs diff=lfs merge=lfs -text
*.umap      filter=lfs diff=lfs merge=lfs -text

# Heavy 3D assets
*.fbx       filter=lfs diff=lfs merge=lfs -text

# Large audio files
*.wav       filter=lfs diff=lfs merge=lfs -text

# Large textures / binary images (only formats commonly used as source files)
*.png       filter=lfs diff=lfs merge=lfs -text
*.tga       filter=lfs diff=lfs merge=lfs -text
*.exr       filter=lfs diff=lfs merge=lfs -text
*.psd       filter=lfs diff=lfs merge=lfs -text

# Video files (often very large)
*.mp4       filter=lfs diff=lfs merge=lfs -text
*.mov       filter=lfs diff=lfs merge=lfs -text

